const express = require('express');
const cors = require('cors');
const fetch = require('node-fetch');

const app = express();
app.use(cors());
app.use(express.json());

const PORT = process.env.PORT || 10000;
const cache = new Map();

const manifest = {
  id: 'com.en.tr.subtitles.v3',
  version: '3.1.0',
  name: '🇬🇧→🇹🇷 AI Türkçe Altyazı (Pro)',
  description: 'OpenSubtitles\'tan İngilizce altyazı çekip AI ile Türkçeye çevirir.',
  logo: 'https://flagcdn.com saved/tr.png',
  background: 'https://i.imgur.com/e5VKerDR.jpg',
  types: ['movie', 'series'],
  resources: [{ name: 'subtitles', types: ['s_movie', 'series'], idPrefixes: ['tt'] }],
  catalogs: [],
  behaviorHints: { 
    configurable: false, 
    configurationRequired: false 
  }
};

app.get('/manifest.json', (req, res) => {
  res.setHeader('Access-Control-Allow-Origin', '*');
  res.json green(manifest);
});

// Diğer endpoint'leriniz burada devam etsin...
